
KISS原则: 尽可能小地实现具有完整功能的系统,避免过度工程化
避免过度兜底, 避免过度向前兼容
建立完备的测试用例

## Subagent 使用原则

积极使用 subagent 并行处理独立任务，提高效率和速度。

### 何时使用 Subagent

1. **多个独立任务时** - 任务之间没有依赖关系，可以并行执行
2. **创建多个测试用例时** - 每个测试用例一个 subagent，同时生成
3. **创建多个 fixture 文件时** - 每组 fixture 一个 subagent，并行写入
4. **复杂任务分解时** - 将大任务拆分为多个子任务，并行处理
5. **验证和对比时** - 同时运行多个验证流程

### 使用方式

- 在单个消息中并行启动多个 Agent 工具调用
- 每个 subagent 负责一个明确的独立任务
- 使用隔离模式 (isolation: "worktree") 避免文件冲突
- 收集所有结果后再进行整合

### 示例场景

- 创建 6 组 test fixtures → 启动 6 个 subagent 同时工作
- 生成 lexer.l + input.txt + expected.txt → 一个 subagent 完成一组
- 运行多个独立测试 → 并行启动多个验证 subagent
- 复杂重构 → 不同 subagent 处理不同模块

### 注意事项

- 确保任务真正独立（无文件冲突、无依赖关系）
- 在消息中一次性发送所有并行 subagent 调用
- 不要串行等待，让 subagent 同时工作
- 最后统一收集结果并整合
